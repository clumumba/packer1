pipeline {
  agent any
 // enviroments{
   //   AWS_ACCESS_KEY = credentials ('jenkins_aws_access_key_id')
     // AWS_SECRET_KEY = credentials ('jenkins_aws_secret_key')
      
 // }
  }
  stages {
    stage("Building AMI") {
      steps {
        sh "packer init aw-ami.pkr.hcl"
        sh "packer build aw-ami.pkr.hcl"
      }
    }
  } 

